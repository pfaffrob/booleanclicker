# This file contains metadata for your plugin.

# This file should be included when you package your plugin.# Mandatory items:

[general]
name=BooleanClicker
qgisMinimumVersion=3.0
description=Use left and right click to create Points with a boolean field.
version=0.3.0
author=Intecrop ltd.
email=robin@intecrop.io

about=Select a point layer with a boolean field, then left click to add True points and right click to add False points. Includes snapping-aware placement preview and optional map recentering.

tracker=http://bugs
repository=http://repo
# End of mandatory metadata

# Recommended items:

hasProcessingProvider=no
changelog=0.3.0 - Added snapping-aware preview marker, optional recenter-on-click with configurable canvas position, persistent dialog selections, and layer filtering to only point layers with boolean fields.

# Tags are comma separated with spaces allowed
tags=python, qgis, digitizing, snapping, boolean, points

homepage=http://homepage
category=Vector
icon=icon.png
# experimental flag
experimental=False

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False

# Since QGIS 3.8, a comma separated list of plugins to be installed
# (or upgraded) can be specified.
# Check the documentation for more information.
# plugin_dependencies=

Category of the plugin: Raster, Vector, Database or Web
# category=

# If the plugin can run on QGIS Server.
server=False

